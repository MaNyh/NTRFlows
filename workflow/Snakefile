from snakemake.utils import validate

configfile : "config/workflow_config.yaml"
validate(config,"schemas/config.schema.yaml")

include: "rules/createcases.smk"

TEMPLATENAME = config["case_type"]
TEMPLATEPATH =f"../resources/{TEMPLATENAME}"

rule all:
    input : *[f"results/simulations/{instance_pattern}/{file}" for instance_pattern in PARAMSPACE.instance_patterns for file in TEMPLATEFILES]
