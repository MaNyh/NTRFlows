$schema: "http://json-schema.org/draft-06/schema#"

description: snakemake configuration file

type: object

properties:
    type: object
    properties:
      case_type:
        type: string
        pattern : openfoamCompressorCascadeRas | trace4avRas
      mesh:
        type: string

    required:
        - case_type
        - mesh
